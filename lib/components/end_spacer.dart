import 'package:flutter/material.dart';

class PageEndSpacer extends StatelessWidget {
  const PageEndSpacer({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 70,
    );
  }
}
